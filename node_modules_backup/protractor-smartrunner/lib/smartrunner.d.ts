import { Logger } from 'protractor/built/logger';
export interface SmartRunnerOptions {
    outputDirectory?: string;
    passedMessagePrefix?: string;
    excludedMessagePrefix?: string;
    repoHash: string;
    exclusionPath?: string | null;
}
export declare class SmartRunner {
    private options;
    private logger;
    private results;
    constructor(options: Required<SmartRunnerOptions>, logger: Logger);
    onPrepare(): void;
    private setupJasmine;
}
